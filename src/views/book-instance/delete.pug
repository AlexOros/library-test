extends ../layout

block content

  h1 !{title}: !{book_instance._id}

  p Do you really want to delete this book instance?

    form(method='POST')
      div.form-group
        input#_id.form-control(type='hidden' name='_id', value=book_instance._id )

      button.btn.btn-primary(type='submit') Delete
